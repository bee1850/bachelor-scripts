#!/bin/bash

chmod +x /root/setup_layer.sh
chmod +x /root/cleanup.sh
chmod +x /root/layer_*/setup.sh
chmod +x /root/baseline/setup.sh
chmod +x /root/monitor.sh
chmod +x /root/benchmark/*.sh
chmod +x /root/benchmark/probes/*.sh
chmod +x /root/info.sh